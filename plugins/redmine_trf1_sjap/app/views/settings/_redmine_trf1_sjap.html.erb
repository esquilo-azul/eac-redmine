<p>
	<label>Usuário administrador</label>
	<select name="settings[admin_user_id]">
		<%= options_for_select(User.all.order('firstname','lastname').collect {|u| [ u.to_s, u.id ] }, settings['admin_user_id'] ) %>
	</select>
</p>

<p>
	<label>Situação de tarefa que indica que ela está bloqueada por outras tarefas ou pela data de início.</label>
	<select name="settings[block_issue_status_id]">
		<%= options_for_select(IssueStatus.all.collect {|p| [ p.name, p.id ] }, settings['block_issue_status_id'] ) %>
	</select>
</p>

<p>
	<label>Situação que a tarefa recebe ao ser desbloqueada.</label>
	<select name="settings[unblock_issue_status_id]">
		<%= options_for_select(IssueStatus.all.collect {|p| [ p.name, p.id ] }, settings['unblock_issue_status_id'] ) %>
	</select>
</p>

<p>
	<label>Mensagem de desbloqueio de tarefa.</label>
	<%= text_area_tag 'settings[unblock_message]', settings['unblock_message'] %>	
</p>

<p>
  <label>Prazo para expiração da consulta de identidades digitais da CEF (Em segundos).</label>
  <%= number_field_tag 'settings[cef_id_solicitacoes_consulta_outdated_seconds]', settings['cef_id_solicitacoes_consulta_outdated_seconds'] %>	
</p>